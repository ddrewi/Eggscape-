using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UI_Cherry : MonoBehaviour
{
    [SerializeField] private Image image;


    // Update is called once per frame
    void Update()
    {
        /*Debug.Log("localScale: " + transform.localScale);
        Debug.Log("" + new Vector3(4f, 4f, 1f));
        if (!transform.localScale.Equals(new Vector3(4f, 4f, 1f)))
        {
            transform.localScale += new Vector3(.1f, .1f, 0);
        }
        else if(!transform.localScale.Equals(new Vector3(3f, 3f, 1f)))
        {
            transform.localScale -= new Vector3(.1f, .1f, 0);
        }*/

    }
}
